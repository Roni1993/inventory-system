query deliveries($page: PageInput) {
    deliveries(page: $page) {
        content {
            id
            status
            category
            plannedDeliveryDate
            actualDeliveryDate
            items {
                name
                quantity
            }
        }
        size
        totalElements
        totalPages
    }
}
